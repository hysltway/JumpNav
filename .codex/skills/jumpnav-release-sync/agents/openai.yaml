interface:
  display_name: "JumpNav Release Sync"
  short_description: "Review JumpNav changes, sync docs, bump version, and create a local commit."
  default_prompt: "Use this skill to review current JumpNav repo changes, update release docs, bump version, and create a detailed Chinese local commit."
